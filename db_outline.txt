SQL

videos
    id
    platform (youtube, tiktok)
    identifier (platform specific id)
    response_id (video can has one response)

video_files
    id
    video_id
    storage_link (gsutil URI)
    format (mp4, ...)

responses
    id
    url
    start_time
    end_time



CREATE TABLE videos(
    id integer SERIAL
    platform

)



NoSQL

videos {
    id
    platform (youtube, tiktok)
    identifier (platform specific id)
    storage_link (gsutil URI)
    format (mp4, ...)
}

responses {
    id
    url
    start_time
    end_time
}
